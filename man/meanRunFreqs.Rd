\name{meanRunFreqs}
\alias{meanRunFreqs}
\title{Create means of frequencies across all runs}
\usage{
  meanRunFreqs(runs.freqs)
}
\description{
  Create means of frequencies across all runs
}
\examples{
runs.freqs <- runs$freqs$all
results$freqs$all <- meanRunFreqs(runsfreqs)
}

